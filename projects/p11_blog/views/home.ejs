<%- include('partials/header'); -%>
<h1>Home</h1>
<p><%= startingContent %></p>

<!-- EJS scriplet tags need to be included to be able to render the javascript -->
<% console.log(posts); %>

<!-- For each element in the array, do an action -->
<!-- Console log the title in the posts array-->
    <% posts.forEach(function(post){ %>
        <h1> <%=post.title%> </h1>
        <p> <%=post.content.substring(0,200) + "..."%> </p>
        <a href="/posts/<%=post._id%>">Read More</a>
    <% }) %>


<%- include('partials/footer'); -%>